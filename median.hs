module Main where 
import System.IO

main = do
    putStrLn ""
    putStrLn "I will compute the median of the list."
	--enteredList : Numlist

--Numlist = do	
	--putStrLn "Enter number (blank line to end): "
	--input <- getLine 
	--if num == "" 
		--then return (inputList)
		
	--else do	
		--enteredNums ++ [num]
		--let num = input : Integer
		--num : inputList
		--nextNum <- Numlist

